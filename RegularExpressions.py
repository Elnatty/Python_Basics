import re

# Regular Expressions
# notes...
"""
--> match(), used to test the input string starts with a specified pattern or not.  
--> search(), to test if specified pattern is present or not in the given input.
--> findall(), to find duplicates in for specified pattern.
--> split(), how to split the input string.
--> sub(), to replace the sub string.
--> compile(), to create pattern object and can be re-used

we will use Raw Strings as r'expression' 
"""